spring:
  application:
    name: job-app
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true